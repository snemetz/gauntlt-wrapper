Feature: OS detection

  Background:
    Given "nmap" is installed
    And the following profile:
      | name     | value            |
      | hostname | <%= @hostname %> |
      | ports    | <%= @port_www %> |

  @slow
  Scenario: Detect OS
    When I launch an "nmap" attack with:
      """
      nmap -sV -p T:<ports> -PN <hostname>
      """
    Then the output should contain:
      """
      Apache
      """

